[project]
name = "decision-mcp-server"
version = "0.9.0"
description = "An MCP Server enabling integration with IBM Decision Server Runtime to retrieve and invoke decision services."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [ "mcp>=1.12.2","requests","pyyaml","jsonref","validator_collection","PyJWT>=2.13.0","cryptography>=48.0.1","starlette>=1.3.1","python-multipart>=0.0.30"]
[[project.authors]]
name = "laurent grateau"
email = "laurent.grateau@fr.ibm.com"

[build-system]
requires = [ "hatchling",]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "cryptography>=48.0.1",
    "idna>=3.15",
    "mcp>=1.28.1,<2.0.0",
    "pydantic-settings>=2.14.2",
    "pyjwt>=2.13.0",
    "pytest>=9.0.3",
    "pytest-asyncio>=1.1.0",
    "pytest-cov>=6.2.1",
    "pytest-html>=4.1.1",
    "python-multipart>=0.0.30",
    "responses>=0.25.8",
    "starlette>=1.3.1",
    "urllib3>=2.7.0",
    "validator-collection>=1.5.0",
]

[project.scripts]
decision-mcp-server = "decision_mcp_server:main"
ibm-odm-decision-mcp-server = "decision_mcp_server:main"
start = "decision_mcp_server:main"